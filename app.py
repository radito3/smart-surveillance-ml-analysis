import main

main.say_hello_to('ZAWARUDO')

# stdin arg: media mtx camera source URL
# create one (or more) FIFO files
# start a 1 second ticker
# start buffering frames in memory
# on every tick, persist frame buffer to the FIFO file, clear the in-memory buffer and start over
# stop on camera disconnect
